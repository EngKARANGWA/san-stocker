import { Injectable } from '@nestjs/common';

/**
 * Scaffolded module for Payroll (salaries, deductions, bonuses, overtime,
 * statutory contributions). Needs PayrollRun/Payslip Prisma models before
 * business logic can be added here.
 */
@Injectable()
export class PayrollService {}

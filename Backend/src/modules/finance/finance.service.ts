import { Injectable } from '@nestjs/common';

/**
 * Scaffolded module for the Financial Management responsibilities in the
 * role matrix (payments, expenses, supplier invoices, tax, bank
 * reconciliation, payroll integration). Needs its own Prisma models
 * (Expense, Invoice, Payment, ...) before business logic can be added here.
 */
@Injectable()
export class FinanceService {}
